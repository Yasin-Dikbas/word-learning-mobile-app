<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_gradient"
    android:padding="24dp">

    <!-- Soru Kartı -->
    <androidx.cardview.widget.CardView
        android:id="@+id/questionCard"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginTop="40dp"
        app:cardCornerRadius="24dp"
        app:cardElevation="8dp"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/tvQuestionWord"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="Apple"
            android:textColor="#1A4B8F"
            android:textSize="36sp"
            android:textStyle="bold" />
    </androidx.cardview.widget.CardView>

    <!-- Şıklar Bölümü -->
    <LinearLayout
        android:id="@+id/optionsContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintBottom_toTopOf="@+id/btnPass"
        app:layout_constraintTop_toBottomOf="@id/questionCard">

        <Button android:id="@+id/btnOption1" style="@style/QuizOptionButtonStyle" android:text="Seçenek 1"/>
        <Button android:id="@+id/btnOption2" style="@style/QuizOptionButtonStyle" android:text="Seçenek 2"/>
        <Button android:id="@+id/btnOption3" style="@style/QuizOptionButtonStyle" android:text="Seçenek 3"/>
        <Button android:id="@+id/btnOption4" style="@style/QuizOptionButtonStyle" android:text="Seçenek 4"/>
    </LinearLayout>

    <!-- Pas Geç Butonu -->
    <Button
        android:id="@+id/btnPass"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?android:attr/selectableItemBackground"
        android:text="Pas Geç / Sonra Dön"
        android:textAllCaps="false"
        android:textColor="#1A4B8F"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginBottom="20dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>